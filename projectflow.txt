1. Create the project template by executing template.py file
2. Create a pyproject.toml file
3. Create a virtual environment, activate it and install the dependencies

----------------------------------------------- MongoDB Setup -----------------------------------------------
4. Sign up to MongoDB Atlas and create a new project by just providing it a name then next next create.
5. From "Create a cluster" screen, hit "create", Select M0 service keeping other services as default, hit "create deployment"
6. Setup the username and password and then create DB user.
7. Go to "network access" and add ip address - "0.0.0.0/0" so that we can access it from anywhere
8. Go back to project >> "Get Connection String" >> "Drivers" >> {Driver:Python, Version:3.6 or later} 
   >> copy and save the connection string with you(replace password). >> Done.
9. Create folder "notebook" >> do step 11 >>  create file "mongoDB_demo.ipynb" >> select kernal>python kernal>vehicle>>
10. Dataset added to notebook folder
11. Push your data to mongoDB database from your python notebook.
12. Go to mongoDB Atlas >> Database >> browse collection >> see your data in key value format

-------------------------------------- logging, exception and notebooks --------------------------------------
13. Write the logger file and test it on demo.py
14. Write the exception file and test it on demo.py
15. EDA and Feature Engg notebook added.

----------------------------------------------- Data Ingestion -----------------------------------------------
16. Before we work on "Data Ingestion" component >> Declare variables within constants.__init__.py file >> 
    add code to configuration.mongo_db_connections.py file and define the func for mondodb connection >> 
    Inside "data_access" folder, add code to proj1_data that will use mongo_db_connections.py
    to connect with DB, fetch data in key-val format and transform that to df >>
    add code to entity.config_entity.py file till DataIngestionConfig class >>
    add code to entity.artifact_entity.py file till DataIngestionArtifact class >>
    add code to components.data_ingestion.py file >> add code to training pipeline >> 
    run demo.py (set mongodb connection url first, see next step)
17. To setup the connection url, open terminal and run below command:
    set: export MONGODB_URL="mongodb+srv://<username>:<password>......"
    check: echo $MONGODB_URL
    Also add "artifact" dir to .gitignore file

----------------------------------------------- DVC Tracking -----------------------------------------------
18. First of all remove the Timestamp logic from src.entity.config_entity.py that creates a new folder inside the `artifacts/` folder everytime we run the pipeline. Because, DVC is going to do the versioning for us now.
19. do a `dvc init`
20. add the s3 remote `dvc remote add artifacts_s3 s3://vehicle-inssurance-artifacts-storages`
21. now that the remote is set, you should tell dvc what it has to track from our local storage which eventually it will push onto the remote. the dvc.yaml and params.yaml files will do this job
22. add code to src.utils.main_utils.py that reads parameters from params.yaml
23. create params.yaml -> add the data_ingestion stage to it -> add the different parameters associated with data_ingestion component (like train_test_split_ration, from where to collect the data and where to store the artifacts of this stage) to this stage.
24. remove the redindant constants from src.constants that you have added as parameters to params.yaml
25. make some associated changes in the src.entity.config_entity.py to also read the values from params.yaml and not just src.constants
26. Now we'll add THE dvc.yaml file
    This file will specify the command to run the stage, the dependencies and the outputs
    Add a main file in the source folder with the same code as in main.py in this repo
    The TrainPipeline.run() method seems to have become of no use with this. But we'll keep it.
27. `dvc repro`
28. `dvc push` and then `git add-commit-push`

---------------------------- Data Validation, Data Transformation & Model Trainer ----------------------------

29. Complete the work on config.schema.yaml file (add entire info about dataset for data validation step)
30. Now work on the "Data Validation" component the way we did in step 17 for Data Ingestion. (Workflow mentioned below)
31. Now work on the "Data Transformation" component the way we did in above step. (add estimator.py to entity folder)
32. Now work on the "Model Trainer" component the way we did in above step. (add class to estimator.py in entity folder)
